apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "facilioo-backend.fullname" . }}-test-connection"
  labels:
    {{- include "facilioo-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "facilioo-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
